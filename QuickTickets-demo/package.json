{
  "name": "quicktickets",
  "version": "1.0.0",
  "description": "Full-stack ticket booking demo app with React frontend and Express backend",
  "main": "index.js",
  "scripts": {
    "install-all": "npm install && cd api && npm install && cd ../frontend && npm install",
    "backend": "cd api && npm start",
    "frontend": "cd frontend && npm start",
    "dev": "concurrently \"npm run backend\" \"npm run frontend\"",
    "start": "npm run dev"
  },
  "keywords": ["react", "express", "tickets", "demo", "fullstack"],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
